# Spring AI OpenAI Configuration (Example)
spring.ai.openai.api-key=YOUR_OPENAI_API_KEY
spring.ai.openai.chat.options.model=gpt-4o # or gpt-3.5-turbo

# Selenium WebDriver Configuration (if needed, e.g., path to driver if not using WebDriverManager)
# selenium.webdriver.chrome.driver=/path/to/chromedriver
